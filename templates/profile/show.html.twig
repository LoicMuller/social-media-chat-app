{% extends 'base.html.twig' %}

{% block title %} {{ parent() }} | Profile {% endblock %}

{% block body %}

    <div class="mb-4">
        <h1 class="h3 mb-0 text-gray-800">
            Profil
        </h1>
    </div>

    <div>
        <p>Prénom : {{ app.user.firstName }}</p>
        <p>Nom : {{ app.user.lastName }}</p>
        <p>Adresse Email : {{ app.user.userIdentifier }}</p>

        <a role="button" class="btn btn-primary" href="{{ path('app_index') }}">Retour</a>
    </div>

{% endblock %}